# 二期落地进度

## 已完成（骨架可跑）

1. **库表**：Alembic `a1b2c3d4e5f6` — 知识库 / Agent / 开关 / 通知 + `suggestion_event` 扩展
2. **功能开关**：`GET/PUT /api/v1/admin/feature-flags`（Redis 秒级）
3. **知识库**：创建文本/上传 → 切分 → **hash embedding + 本地向量库** → 发布 → 向量+关键词混合检索；开班计划强制 `confirmPriceRisk`
4. **回复分流**：`/reply/suggestions/stream` → `legacy` / `rag` / `agent`
5. **Agent**：步骤 SSE、打断、反馈、Run 详情；**规划/综合迁至 ai_runtime LangGraph**，业务只转发 SSE + 落库
6. **通知**：知识库发布后写 `advisor_notification`；`GET /users/me/notifications`
7. **AI Runtime**：`:9000` — DeepSeek InferReply + **`/v1/rag/answer`** + **`/v1/agent/plan|synthesize`（LangGraph）**
8. **侧边栏**：消费 `agent_start` / `agent_step` / `rag_status` / citations / uncertaintyNotes；打断与「有问题」反馈
9. **演示闭环**：侧边栏左侧「模拟家长对话」+ 企微占位条；采纳建议写回模拟窗

## 接下来

0. ~~演示闭环~~
1. ~~RAG 变真~~（本地向量库 + `/v1/rag/answer`；Milvus 可后续替换 `LocalVectorStore`）
2. ~~Agent → LangGraph~~
3. **企微真接**：有账号再开；保留模拟窗开关 `DEMO_PARENT_CHAT`

## 本地升级 / 联调

```bash
alembic upgrade head
pip install -r requirements.txt
cd ai_runtime && pip install -r requirements.txt && cd ..
pytest -q

# DeepSeek 联调（三进程）
# A: cd ai_runtime && uvicorn app.main:app --port 9000
# B: AI_MODE=remote AI_REMOTE_BASE_URL=http://127.0.0.1:9000 uvicorn app.main:app --port 8000
# C: cd sidebar && npm run dev
# 打开 http://127.0.0.1:5173/?customerId=3&conversationId=2
```
